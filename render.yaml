services:
  - type: web
    name: lufilya-school-management
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /api/state
    envVars:
      - key: USE_SUPABASE
        value: false
